# Secrets and machine-local overrides — never commit
shell/secret.zsh
*.local.zsh
.zshlocalrc
.env
.env.*
*.pem
*.key
id_rsa
id_ed25519

# macOS junk
.DS_Store

# Legacy local-only directory
_other

# Local-only AI assistant guidance (per-project notes stay untracked,
# but the global CLAUDE.md at claude/ is symlinked to ~/.claude/)
CLAUDE.md
!claude/CLAUDE.md
