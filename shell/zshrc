# Path to your oh-my-zsh configuration.
export DOTFILES=$HOME/.dotfiles
ZSH=$DOTFILES/oh-my-zsh
ZSH_CUSTOM=$DOTFILES/shell/custom-oh-my-zsh
ZSH_THEME="cwoebker"

# oh-my-zsh configuration
DISABLE_AUTO_UPDATE="false"
COMPLETION_WAITING_DOTS="true"

# fzf (installed via Homebrew)
export FZF_BASE="${HOMEBREW_PREFIX:-/usr/local}/opt/fzf"

plugins=(
	brew
	git git-extras github
	macos
	fzf
	docker docker-compose
)

source $ZSH/oh-my-zsh.sh
source $DOTFILES/shell/path.zsh
source $DOTFILES/shell/xdg.zsh
source $DOTFILES/shell/config.zsh
source $DOTFILES/shell/functions.zsh
# all of our zsh files
typeset -U alias_files
alias_files=($DOTFILES/**/aliases.zsh)
for file in ${alias_files}
do
  source $file
done
unset alias_files

# activate zsh completions
fpath=(/usr/local/share/zsh-completions $fpath)

# use ~/.zshlocalrc for machine-local and work-related settings
# (secrets, work aliases, machine-specific paths) — never tracked.
if [[ -a ~/.zshlocalrc ]]
then
  source ~/.zshlocalrc
fi
